package com.example.opencv_tutorial

/**
 * Build configuration flags for debugging and performance measurement
 * Matches the C++ configuration in Config.hpp
 */
object BuildConfig {
    // Whether to enable debug logging (matches DEBUG_MODE in C++)
    const val DEBUG = true

    // Whether to enable performance timing measurements (matches TIMING_MODE in C++)
    const val TIMING_MODE = true
}
